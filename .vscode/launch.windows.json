{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./build/shmupwarz.exe",
            "cwd": "${workspaceRoot}"
        }
    ]
}